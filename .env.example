# Supabase project settings: Project Settings → API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Cloudinary: Dashboard for cloud name; Settings → Upload → Upload presets
# for an UNSIGNED preset (client-side uploads never use the API secret).
VITE_CLOUDINARY_CLOUD_NAME=
VITE_CLOUDINARY_UPLOAD_PRESET=
